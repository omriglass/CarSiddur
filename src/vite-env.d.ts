/// <reference types="vite/client" />

/**
 * Baked in by `vite.config.ts`'s `define` at build time: release tag / short
 * commit shown on the profile screen's version footer (docs/RUNBOOK_ROLLBACK.md).
 */
declare const __APP_VERSION__: string;
